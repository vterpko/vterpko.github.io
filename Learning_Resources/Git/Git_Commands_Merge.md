Merge changes from **a branch** into current branch.  
Use `‑‑no-commit` to leave changes uncommitted. Use `--no-ff` to create a merge commit even if the merge resolves as a fast forward.

```
git merge <commit/branch>
```

#git  #howto 